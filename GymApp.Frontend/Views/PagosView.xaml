<UserControl x:Class="GymApp.Frontend.Views.PagosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:GymApp.Frontend.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:PagosViewModel}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ENCABEZADO -->
        <TextBlock Grid.Row="0" 
                   Text="💰 Gestión de Pagos" 
                   FontSize="26" 
                   FontWeight="Bold"
                   Foreground="#2C3E50"
                   Margin="0,0,0,15"/>

        <!-- TARJETAS DE TOTALES -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Total mes actual -->
            <Border Grid.Column="0"
                    Background="#27AE60"
                    CornerRadius="6"
                    Padding="20,15"
                    Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="📅 Ingresos del Mes" 
                               Foreground="White" 
                               FontSize="13"
                               Opacity="0.9"/>
                    <TextBlock Text="{Binding TotalMesActual, StringFormat=C}" 
                               Foreground="White" 
                               FontSize="26"
                               FontWeight="Bold"
                               Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <!-- Total general -->
            <Border Grid.Column="1"
                    Background="#3498DB"
                    CornerRadius="6"
                    Padding="20,15"
                    Margin="8,0,0,0">
                <StackPanel>
                    <TextBlock Text="💵 Ingresos Totales" 
                               Foreground="White" 
                               FontSize="13"
                               Opacity="0.9"/>
                    <TextBlock Text="{Binding TotalGeneral, StringFormat=C}" 
                               Foreground="White" 
                               FontSize="26"
                               FontWeight="Bold"
                               Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- BOTONES -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    Margin="0,0,0,15">
            <Button Content="➕ Registrar Pago" 
                    Command="{Binding NuevoCommand}"
                    Style="{StaticResource PrimaryButtonStyle}"/>
            <Button Content="🗑️ Eliminar" 
                    Command="{Binding EliminarCommand}"
                    Style="{StaticResource DangerButtonStyle}"/>
            <Button Content="🔄 Refrescar" 
                    Command="{Binding CargarCommand}"
                    Style="{StaticResource PrimaryButtonStyle}"/>
        </StackPanel>

        <!-- TABLA -->
        <Border Grid.Row="3" 
                Background="White" 
                CornerRadius="6"
                BorderBrush="#E0E0E0"
                BorderThickness="1">
            <DataGrid ItemsSource="{Binding Pagos}"
                      SelectedItem="{Binding PagoSeleccionado}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#EEEEEE"
                      RowHeight="38"
                      FontSize="13"
                      BorderThickness="0"
                      RowBackground="White"
                      AlternatingRowBackground="#F9F9F9">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                    <DataGridTextColumn Header="Socio" Width="*">
                        <DataGridTextColumn.Binding>
                            <MultiBinding StringFormat="{}{0} {1}">
                                <Binding Path="Socio.Nombre"/>
                                <Binding Path="Socio.Apellido"/>
                            </MultiBinding>
                        </DataGridTextColumn.Binding>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Monto" 
                                        Binding="{Binding Monto, StringFormat=C}" 
                                        Width="110"/>
                    <DataGridTextColumn Header="Fecha" 
                                        Binding="{Binding FechaPago, StringFormat=dd/MM/yyyy HH:mm}" 
                                        Width="150"/>
                    <DataGridTextColumn Header="Método" Binding="{Binding MetodoPago}" Width="130"/>
                    <DataGridTextColumn Header="Observaciones" Binding="{Binding Observaciones}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>